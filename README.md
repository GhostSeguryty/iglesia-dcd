# Discipulado Cristiano de Durango

Página pública con servidor. Todos entran sin login.
El administrador entra con **Acceso** arriba a la derecha.

## Local
```
npm install
npm start
```
Abre http://localhost:3000

## Render
Igual que Cewyx:
- Web Service, Node, `npm install`, `node server.js`
- Conecta Postgres y `DATABASE_URL` para que no se borre el admin
